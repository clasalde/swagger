

<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Profile</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .profile-container {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .profile-card {
            max-width: 400px;
            padding: 20px;
            border: 1px solid #ddd;
            border-radius: 10px;
            background-color: #fff;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            text-align: center;
        }

        .profile-card h1 {
            font-size: 1.5rem;
            margin-bottom: 20px;
            color: #343a40;
        }

        .profile-card p {
            font-size: 1rem;
            margin-bottom: 10px;
            color: #6c757d;
        }

        .profile-card .btn {
            margin-top: 10px;
            width: 100%;
            font-size: 1rem;
            font-weight: 500;
        }
    </style>
</head>

<body>
    <div class="container profile-container">
        <div class="profile-card">
            <h1>My Profile</h1>
            <p>Welcome, {{user.first}} {{user.last}}!</p>
            <p>Role: {{user.role}}</p>

            <form action="/api/users/logout" method="POST">
                <button class="btn btn-primary" type="submit">Logout</button>
            </form>

            <div class="mt-3">
                {{#if this.isAdmin}}
                <a class="btn btn-secondary" href="/admin">Admin Panel</a>
                {{else}}
                <a class="btn btn-secondary" href="/chat">Live Chat</a>
                <a class="btn btn-secondary" href="/products">Store</a>
                <a class="btn btn-secondary" href="/passwordreset">Change Password</a>
                {{/if}}
            </div>
        </div>
    </div>

    <!-- Bootstrap JavaScript (optional, if you need it) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>